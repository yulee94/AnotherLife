# Moonroot Floodbasin Visual Source

This folder contains the exact A2 terrestrial-design companion source for one
Eldergrove habitat and its paired supporting fauna:

- habitat: `tdf_habitat_eldergrove_moonroot_floodbasin`;
- environment kit: `tdf_envkit_eldergrove_moonroot_floodbasin`;
- fauna: `tdf_fauna_eldergrove_moonshell_cicada`;
- visualized fauna ecotype: `moonshell_flood_season`;
- source version: `tdf-eco-moonroot-2026-07-27-v001`;
- parent roster: `tdf-eco-2026-07-27-v001`.

The parent roster remains immutable. This companion advances the exact
`moonroot_deep_flood` habitat source and the pictured `moonshell_flood_season`
ecotype to `ReadyForUserReview`. The `moonshell_dry_season` ecotype remains
`ProposedTextOnly`.

## Contents

- `Moonroot_Floodbasin_And_Moonshell_Cicada_Visual_Source.md` — protected
  identities, views, materials, motion, habitat composition, reuse, quality
  reductions, provisional production envelopes, and non-authority.
- `Executed_Generation_Prompts_And_Provenance.md` — exact executed prompts,
  internal input/output lineage, generated-original evidence names, and
  retention decisions.
- `Visual_QA_Disposition.md` — direct pixel review, per-asset disposition,
  required corrections, and the production block.
- `moonroot_floodbasin_visual_source_manifest.json` — exact IDs, paths,
  hashes, LFS identity, budgets, source states, and generation records.
- `moonroot_floodbasin_visual_source_packet.schema.json` — strict retained
  structural schema for this exact packet.
- `ConceptSheets/` — six accepted review-source PNGs.
- `GenerationInputs/` — three generated bases retained because each directly
  produced an accepted refined output.

## State

- Technical packet: `TechnicalReviewReady`
- Habitat source: `ReadyForUserReview`
- Flood-season fauna source: `ReadyForUserReview`
- Overall visual QA: `PassWithConcern`
- User creative state: `NotRequested`
- Narrative naming: `WorkingLabelsOnly`
- Runtime/production integration: `Blocked`

The concerns are not hidden acceptance. Repeated stone cadence can still read
as constructed, the split buttress can drift toward a gate, the feeding rostrum
needs a non-avian production lock, and the most reduced habitat thumbnail loses
the fauna presence. These are explicit correction gates before production.

## Source Budget

- Final PNGs: `6`
- Retained inputs: `3`
- Final bytes: `16,921,209`
- Retained-input bytes: `8,758,710`
- Total source bytes: `25,679,919`
- Per-image ceiling: `4,194,304`
- Six-final ceiling: `25,165,824`
- Three-input ceiling: `12,582,912`
- Wave ceiling: `37,748,736`
- Player/install contribution: `0`
- Runtime-resident contribution: `0`

Every raster is `1536 × 1024`, opaque 8-bit RGB PNG, stored through Git LFS
under `unity/Docs`. No duplicate thumbnail, editable-source fiction, model,
texture set, rig, animation, prefab, scene, shader, marketplace archive, or
runtime catalog is included.

## Authority Boundary

This packet owns visual-source identity, not runtime behavior. It does not
authorize terrain tiles, routes, colliders, spawning, population, AI, combat,
stats, loot, quests, lore, seasons, localization, save data, streaming,
Addressables, device support, or production asset integration.

The user must approve the exact source version, IDs, variants, files, hashes,
and accepted concerns before production. A separate Codex coordination/review
handoff must define measured camera distance, LOD thresholds, streaming,
pooling, memory, failure behavior, and acceptance captures before engineering.
